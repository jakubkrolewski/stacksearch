<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="question"
            type="pl.jkrolewski.stacksearch.search.model.Question"/>
    </data>

    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="120dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp">

            <ImageView
                android:id="@+id/avatar"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                tools:src="@mipmap/ic_launcher"/>

            <TextView
                android:layout_toRightOf="@id/avatar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:lines="3"
                style="@style/Base.TextAppearance.AppCompat.Title"
                android:ellipsize="end"
                android:text="@{question.title}"
                tools:text="How should I write stack search application that looks fancy?"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="FancyUser333"
                android:text='@{question.owner != null ? question.owner.displayName : ""}'/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="99"
                android:text='@{String.valueOf(question.answerCount)}'/>
        </RelativeLayout>
    </android.support.v7.widget.CardView>
</layout>
